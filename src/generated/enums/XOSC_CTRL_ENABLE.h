/**
 * \file
 * \brief Generated by ifgen (4.9.0).
 */

#pragma once
#ifndef RP2040_ENUMS_XOSC_CTRL_ENABLE_H
#define RP2040_ENUMS_XOSC_CTRL_ENABLE_H

#include "../ifgen/common.h"
#include <cstdint>
#include <cstring>

namespace RP2040
{

enum class XOSC_CTRL_ENABLE : uint16_t
{
    DISABLE = 3358,
    ENABLE = 4011
};
static_assert(sizeof(XOSC_CTRL_ENABLE) == 2);

/**
 * Converts XOSC_CTRL_ENABLE to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(XOSC_CTRL_ENABLE instance)
{
    const char *result = "UNKNOWN XOSC_CTRL_ENABLE";

    switch (instance)
    {
    case XOSC_CTRL_ENABLE::DISABLE:
        result = "DISABLE";
        break;
    case XOSC_CTRL_ENABLE::ENABLE:
        result = "ENABLE";
        break;
    }

    return result;
}

inline std::ostream &operator<<(std::ostream &stream,
                                XOSC_CTRL_ENABLE instance)
{
    stream << to_string(instance);
    return stream;
}

/**
 * Converts a C string to XOSC_CTRL_ENABLE.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, XOSC_CTRL_ENABLE &output)
{
    bool result = false;

    if ((result = !strncmp(data, "DISABLE", 7)))
    {
        output = XOSC_CTRL_ENABLE::DISABLE;
    }
    else if ((result = !strncmp(data, "ENABLE", 6)))
    {
        output = XOSC_CTRL_ENABLE::ENABLE;
    }

    return result;
}

}; // namespace RP2040

#endif
